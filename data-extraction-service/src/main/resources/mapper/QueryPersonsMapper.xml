<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.enc.dao.mapper.QueryPersonsMapper" >

    <insert id="addQueryPersons" parameterType="com.enc.domain.access.entity.QueryPersonsEntity" >
        insert into t_query_persons(
            person_id,
            area_id,
            room_id,
            person_name,
            social_type,
            social_number,
            landlord_relative,
            photo,
            phone,
            reg_time
        )
        VALUES (
            #{personId},
            #{areaId},
            #{roomId},
            #{personName},
            #{socialType},
            #{socialNumber},
            #{landlordRelative},
            #{photo},
            #{phone},
            #{regTime}
        )
    </insert>


</mapper>